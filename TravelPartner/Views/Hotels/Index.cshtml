@model IEnumerable<TravelPartner.Data.Entities.Hotel>

@*<div class="container">
        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-12 top-buffer">
                    <div class="media">
                        <div class="media-left">
                            <div class="col-md-2">
                                <a href="#">
                                    <img src="~/Images/image1.jpe" />


                                </a>
                            </div>
                            <div class="col-md-8">
                                <h4 class="media-heading">@item.Title</h4>
                                @item.Content
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>*@
<div class="container" style="margin-top:90px; height:auto; position:relative">
    @foreach (var item in Model)
    {
        <div style="margin:5px,5px,5px,5px">
            <div class="col-sm-6 col-md-4">
                <div class="thumbnail">
                    <img src="~/Images/image1.jpe" />
                    <div class="caption">
                        <h3>@item.Title</h3>
                        <p>@item.Content</p>
                        <p class="col-lg-offset-5"><a href="@item.Url" class="btn btn-primary" target="_blank" role="button">Open</a> </p>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
